spring:
  application:
    name: Topic Stream Consumer

  cloud:
    stream:
      kafka:
        binder:
          brokers: localhost:9092

      bindings:
        input:
          destination: greetings1
          contentType: application/json

server:
  port: 8006